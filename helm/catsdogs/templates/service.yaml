apiVersion: v1
kind: Service
metadata:
  name: {{ include "catsdogs.fullname" . }}-svc
  labels:
    {{- include "catsdogs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "catsdogs.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
